Read:
1) SESSION_HANDOFF.md
2) RUNBOOK.md
3) logs/run_history.csv
4) latest replay in logs/game_*.jsonl
5) run_bot.py (focus on _select_dropoff_queue_leader, _decide_one, _build_greedy_assignments)
6) past-championships-data/Grocery-bot-Game-AI-documentation-2026.txt (game API reference)

Objective:
Score Expert (10 bots, 28x18) for the first time — the only remaining map.

Context:
- Easy: 128. Medium: 99. Hard: 71 (deterministic). All confirmed.
- Leaderboard = sum of best score across all 4 difficulty maps. Current total: 298 (rank 40).
- Bot scales from 1-5 bots without code changes. Expert (10 bots) is untested.
- Key Expert risks: drop-off congestion with 10 bots, BFS timing on 28x18 grid.
- Game is deterministic per day (same seed = same result).
- Rate limits: 60s cooldown, 40/hour, 300/day per team.

Immediate steps:
1. Get Expert token, run bot. Record score and action counts.
2. If score is reasonable (>50), move to Easy next.
3. If score is low, analyze replay: check drop_off count, wait count, pickup waste.
4. If drop-off bottleneck confirmed, consider increasing queue size from 2 to 3.
5. After Expert + Easy, run Medium once to confirm stability (optional, deterministic).

Deliverables after each run:
- Score + action counts summary
- Known bottleneck (if any)
- Whether code changes are needed before next run
- Updated SESSION_HANDOFF.md
